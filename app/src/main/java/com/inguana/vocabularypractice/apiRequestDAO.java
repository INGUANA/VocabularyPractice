package com.inguana.vocabularypractice;

import retrofit2.Call;
import retrofit2.Callback;
import retrofit2.Response;

public class apiRequestDAO {

    /*APIInterface APIInterface = JsonGetter.buildService(APIInterface.class);
    Call<String> call = APIInterface.getWordTranslation();

    public void getTranslationRequest() {
        call.enqueue(new Callback<String>() {
            @Override
            public void onResponse(Call<String> request, Response<String> response) {
                //do whatever with response
            }

            @Override
            public void onFailure(Call<String> request, Throwable t) {
                //do whatever with response (error)
            }
        });
    }*/
}
